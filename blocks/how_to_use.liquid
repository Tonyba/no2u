{% stylesheet %}
  .how-to-use .texts {
    padding-right: 40px;
  }

  .how-to-use .step {
    width: 60px;
    height: 60px;
  }
{% endstylesheet %}

{% liquid
  assign items = product.metafields.custom.how_to_use_items.value
%}

<div class="grid-{{ items | size }} cols-2-t cols-1-m">
  {% for item in items %}
    {% assign split = item | split: '|' %}
    {% assign title = split.first %}
    {% assign content = split.last %}
    {% assign step = forloop.index %}

    <div class="col">
      <div class="flex">
        <div class="step">
          {{ step }}
        </div>
        <div class="divider"></div>
      </div>

      <div class="texts">
        <h3>{{ title }}</h3>
        <p>{{ content }}</p>
      </div>
    </div>
  {% endfor %}
</div>

{% schema %}
{
  "name": "How to use",
  "class": "how-to-use",
  "settings": [],
  "presets": [
    {
      "name": "how to use"
    }
  ]
}
{% endschema %}
